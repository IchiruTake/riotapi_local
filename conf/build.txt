# For local reproduction,
python -m pip install fastapi "uvicorn[standard]" python-multipart tzdata logfire httpx httpx[http2] httpx[socks] itsdangerous

# For SSL key generation
openssl req -x509 -newkey rsa:4096 -sha256 -days 3650 -nodes -keyout ca.key -out ca.crt -subj "/C=XX/ST=US/L=New York/O=OneMember/OU=Transformer/CN=riotapi-test.com" -addext "subjectAltName=DNS:riotapi-test.com,DNS:*.riotapi-test.com,IP:127.0.0.1"

# For testing

